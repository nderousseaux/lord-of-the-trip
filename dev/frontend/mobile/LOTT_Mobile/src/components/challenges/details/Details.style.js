import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    mainContainer: {
        height:100,
        backgroundColor: 'red',
        marginBottom: 10,

        alignItems: 'center',
        justifyContent: 'center'

    },
});