import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    mainContainer: {
        marginBottom: 20
    },

    descriptionText:{
        textAlign: 'center',
        fontSize:15
    },
});